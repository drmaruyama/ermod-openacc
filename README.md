This package provides an OpenACC version of ERmod.
It includes two programs, `erdst` and `slvfe`, for GPU-accelerated
solvation free energy calculations based on the energy representation method.
For details on how to use ERmod and  information about the original code, please visit https://sourceforge.net/projects/ermod/.

ERmod (Energy Representation Module) is a program for calculating solvation free energies based on the energy representation method. It allows users to calculate the solvation free energies in arbitrary solvents, including inhomogeneous systems, and works in conjunction with state-of-the-art molecular simulation software, such as NAMD, GROMACS, and AMBER.

The `erdst` program calculates the energy distribution functions required for energy representation theory from molecular simulation trajectories.
The `slvfe` program calculates solvation free energies from the energy distribution functions generated by erdst.

## Requirements
- NVIDIA HPC SDK (https://developer.nvidia.com/hpc-sdk)

## Typical Installation
This package uses Autotools for its build system. To compile the program,
1. `cd erdst` (or `cd slvfe`)
2. Run `autoreconf -fiv`.
3. Run `configure` with the appropriate options (see below).
4. Run `make` to compile the program.

## Configuration
### erdst
For multi-GPU execution:
```
$ ./configure FC=nvfortran CC=nvc MPIFC=mpif90
```
or for single-GPU,
```
$ ./configure --disable-mpi FC=nvfortran CC=nvc
```

Single-precision calculations are enabled by default.
To enable double-precision calculations: 
```
$ ./configure --enable-double [other options]
```

If the configuration completes, run:
```
$ make
```
to start the compilation.

### slvfe
The `slvfe` command currently supports single-GPU execution only:
```
$ ./configure FC=nvfortran
```
If the configuration completes successfully, run:
```
$ make
```
to start the compilation.

The current configuration script supports only `nvfortran` as the compiler.

## Reference for ERmod-OpenACC
ERmod-OpenACC: GPU Acceleration of Solvation Free Energy Calculation with Energy-Representation Theory.

Yutaka Maruyama, Hidekazu Kojima, Nobuyuki Matubayasi,
J. Comput. Chem. **46**, e70152 (2025). 
https://doi.org/10.1002/jcc.70152

## Reference for the truncated HNC (tHNC) functional
Truncated Hypernetted Chain Approximation: Accurate Functional for Energy Representation Theory

Yutaka Maruyama, Ryoma Kaji, Nobuyuki Matubayasi,
J. Chem. Theory Comput. **21**, 10503–10514 (2025)
https://doi.org/10.1021/acs.jctc.5c01239

## Reference for the Multi-Input Linear Correction method
Extending Multi-Input Linear Correction to Energy Representation Theory: Accurate Solvation Free Energy Prediction Independent of Volume Information

Yutaka Maruyama, Nobuyuki Matubayasi, Norio Yoshida,
J. Phys. Chem. B. **130**, 6185-6197 (2026)
https://doi.org/10.1021/acs.jpcb.6c02734
